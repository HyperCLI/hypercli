---
title: hyper user
description: Show the current authenticated user.
---

## user

```bash
hyper user
hyper user --output json
```

`hyper user` is implemented as the command group's callback, so running the group itself prints the current user record.
