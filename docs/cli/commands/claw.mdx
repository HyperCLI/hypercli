---
title: hyper claw
description: Deprecated documentation page kept for compatibility.
---

`hyper claw` has been replaced by [`hyper agent`](/cli/commands/agent).

Use these commands instead:

```bash
hyper agent plans
hyper agent subscribe 1aiu
hyper agent status
hyper agent config env
hyper agent openclaw-setup
```
