---
title: CLI
description: Detailed overview of the Python CLI for HyperCLI platform APIs, HyperCLI plans, OpenClaw deployments, backend shells/logs, and gateway operations.
---

## Install

```bash
pip install hypercli-cli
```

Optional extras:

```bash
pip install "hypercli-cli[wallet]"
pip install "hypercli-cli[comfyui]"
pip install "hypercli-cli[stt]"
```

## Command Groups

Every registered top-level command maps to one detailed reference:

| Command | Purpose | Reference |
| --- | --- | --- |
| `hyper configure` | Save the product key and API base | [Configuration](/cli/configuration) |
| `hyper me` | Resolve auth context and agent entitlement summary | [Configuration](/cli/configuration#diagnosing-401-errors) |
| `hyper status` | Query compact model and cluster health | [Configuration](/cli/configuration) |
| `hyper launch` | Alias for `hyper instances launch` | [instances](/cli/commands/instances) |
| `hyper agents` | Managed agent lifecycle, access, gateway, and web search | [agents](/cli/commands/agents) |
| `hyper agent` | Account, subscription, onboarding, and compatibility tools | [agent](/cli/commands/agent) |
| `hyper billing` | Balance, transactions, and invoices | [billing](/cli/commands/billing) |
| `hyper comfyui` | Run and manage ComfyUI workflow jobs | [comfyui](/cli/commands/comfyui) |
| `hyper files` | Upload, inspect, and delete flow inputs | [files](/cli/commands/files) |
| `hyper flow` | Submit and inspect simplified media flows | [flow](/cli/commands/flow) |
| `hyper instances` | Discover capacity and launch GPU instances | [instances](/cli/commands/instances) |
| `hyper keys` | Create, list, and disable API keys | [keys](/cli/commands/keys) |
| `hyper jobs` | Inspect and operate GPU jobs | [jobs](/cli/commands/jobs) |
| `hyper llm` | Direct chat and local-image inference | [llm](/cli/commands/llm) |
| `hyper memory` | Prepare OpenClaw memory artifacts | [memory](/cli/commands/memory) |
| `hyper user` | Show the current user account | [user](/cli/commands/user) |
| `hyper voice` | TTS, voice cloning/design, and local transcription | [voice](/cli/commands/voice) |
| `hyper wallet` | Local x402 wallet and wallet login | [wallet](/cli/commands/wallet) |
| `hyper workspaces` | Shared knowledge, files, search, sync, and grants | [workspaces](/cli/commands/workspaces) |

## Split Between `hyper agent` And `hyper agents`

Use:

- `hyper agent`
  - account setup
  - plans
  - login / subscribe
  - embed helpers
- `hyper voice`
  - text-to-speech
  - voice cloning / design
  - local FasterWhisper transcription
- `hyper agents`
  - actual agent lifecycle
  - files
  - exec
  - backend shell/log streams
  - gateway config/chat/session/files/cron

## Current Agent Route Model

The CLI now follows the same route defaults as the SDKs:

- root host = OpenClaw gateway / agent
- `desktop-<host>` = desktop when launched with `--desktop`
- there is no default `shell-<host>` route

So:

- `hyper agents shell` uses backend `/ws/shell/{agent_id}`
- `hyper agents logs --ws` uses backend `/ws/logs/{agent_id}`
- desktop/browser launches use the OpenClaw pro image and set `OPENCLAW_DESKTOP_ENABLED=1`

## Recommended Reading

- [CLI Quickstart](/cli/quickstart)
- [hyper keys](/cli/commands/keys)
- [hyper llm](/cli/commands/llm)
- [hyper voice](/cli/commands/voice)
- [hyper agent](/cli/commands/agent)
- [hyper agents](/cli/commands/agents)
