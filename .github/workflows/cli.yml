name: CLI CI
run-name: CLI CI

# One gate per command group (checks read `cli / agents`, `cli / jobs`, ...),
# plus a subcommand-matrix smoke per subcommand (checks read
# `cli / agents / chat`, ...). The matrix is generated from
# cli/tests/ci-matrix.json by cli/scripts/expand-ci-matrix.mjs — adding a
# subcommand means adding a usage line (public) or a HIDDEN entry plus one
# manifest line; cli/tests/ci-matrix.test.ts fails the run when the manifest
# drifts from the registry.
#
# No npm cache uploads. The image job builds/pushes docker/cli-ci.Dockerfile
# (ts-sdk + cli deps, dists, and tests baked in; per-gate runner scripts at
# /tests) tagged with the short commit SHA; every test job runs one
# `docker run <image> /tests/<gate>.sh [args]`. Registry coordinates come
# from CI secrets, not this file.
#
# All jobs run on the self-hosted Linux runners. Windows is not gated.
#
# Coordination uses a GitHub concurrency group (no runner-local lock files or
# flock, per AGENTS.md).

on:
  push:
    branches: [main, dev]
    paths:
      - ".github/workflows/cli.yml"
      - "docker/cli-ci.Dockerfile"
      - ".github/scripts/cli-ci/**"
      - "cli/**"
      - "skills/**"
      - "ts-sdk/**"
  pull_request:
    paths:
      - ".github/workflows/cli.yml"
      - "docker/cli-ci.Dockerfile"
      - ".github/scripts/cli-ci/**"
      - "cli/**"
      - "skills/**"
      - "ts-sdk/**"
  workflow_dispatch:

concurrency:
  group: cli-ci-${{ github.ref }}
  cancel-in-progress: true

defaults:
  run:
    shell: bash

env:
  REGISTRY: ${{ secrets.CI_REGISTRY }}
  REGISTRY_ORG: ${{ secrets.CI_REGISTRY_ORG }}
  CLI_CI_IMAGE_REPOSITORY: hypercli-cli-ci

jobs:
  # Builds (or reuses) the shared CI image. Tagged with the short commit SHA
  # (same as hyperclaw-backend); the image is rebuilt when it is absent from
  # the registry for this SHA.
  image:
    name: cli / image
    runs-on: self-hosted
    timeout-minutes: 30
    outputs:
      image_tag: ${{ steps.meta.outputs.image_tag }}
    steps:
      - uses: actions/checkout@v4

      - name: Compute image tag
        id: meta
        run: |
          set -euo pipefail
          TAG="$(echo "${{ github.sha }}" | cut -c1-7)"
          echo "image_tag=${TAG}" >> "${GITHUB_OUTPUT}"

      - name: Login to registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ secrets.CI_REGISTRY_USER }}
          password: ${{ secrets.CI_REGISTRY_TOKEN }}

      - name: Build and push image if absent
        run: |
          set -euo pipefail
          IMAGE="${REGISTRY}/${REGISTRY_ORG}/${CLI_CI_IMAGE_REPOSITORY}:${{ steps.meta.outputs.image_tag }}"
          if docker manifest inspect "${IMAGE}" >/dev/null 2>&1; then
            echo "image already present: ${IMAGE}"
            exit 0
          fi
          docker build \
            -f docker/cli-ci.Dockerfile \
            -t "${IMAGE}" \
            .
          docker push "${IMAGE}"

  # Expands the smoke matrix. Runs on the runner so the smoke matrix can
  # reference `needs.expand.outputs.smokes`.
  expand:
    name: cli / expand
    runs-on: self-hosted
    timeout-minutes: 5
    outputs:
      smokes: ${{ steps.matrices.outputs.smokes }}
    steps:
      - uses: actions/checkout@v4
      - name: Expand cli/tests/ci-matrix.json into the smoke matrix
        id: matrices
        working-directory: cli
        run: node scripts/expand-ci-matrix.mjs

  # One gate per command group: the group's vitest files plus the bare
  # `node dist/index.js <group> --help` smoke, inside the shared image.
  gate:
    name: cli / ${{ matrix.group }}
    needs: [image, expand]
    runs-on: self-hosted
    timeout-minutes: 20
    strategy:
      fail-fast: false
      matrix:
        include:
          - group: core
          - group: me
          - group: configure
          - group: skills
          - group: agents
          - group: jobs
          - group: flow
          - group: files
          - group: voice
    steps:
      - uses: actions/checkout@v4

      - name: Login to registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ secrets.CI_REGISTRY_USER }}
          password: ${{ secrets.CI_REGISTRY_TOKEN }}

      - name: Group vitest files
        run: docker run --rm "${REGISTRY}/${REGISTRY_ORG}/${CLI_CI_IMAGE_REPOSITORY}:${{ needs.image.outputs.image_tag }}" /tests/gate.sh "${{ matrix.group }}"

      - name: Bare group --help smoke (offline, no API key)
        run: docker run --rm "${REGISTRY}/${REGISTRY_ORG}/${CLI_CI_IMAGE_REPOSITORY}:${{ needs.image.outputs.image_tag }}" /tests/gate-smoke.sh "${{ matrix.group }}"

  # Smoke matrix: every non-bare smoke in the manifest, one check per
  # subcommand (grouped by command group in the run graph).
  smoke:
    name: cli / ${{ matrix.group }} / ${{ matrix.sub }}
    needs: [image, expand]
    runs-on: self-hosted
    timeout-minutes: 10
    strategy:
      fail-fast: false
      matrix:
        include: ${{ fromJSON(needs.expand.outputs.smokes) }}
    steps:
      - uses: actions/checkout@v4

      - name: Login to registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ secrets.CI_REGISTRY_USER }}
          password: ${{ secrets.CI_REGISTRY_TOKEN }}

      - name: Offline --help smoke
        run: docker run --rm "${REGISTRY}/${REGISTRY_ORG}/${CLI_CI_IMAGE_REPOSITORY}:${{ needs.image.outputs.image_tag }}" /tests/smoke.sh ${{ matrix.argv }}

  # npm pack --dry-run gate: tarball contents + LF shebang.
  pack:
    name: cli / pack
    needs: [image, expand]
    runs-on: self-hosted
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4

      - name: Login to registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ secrets.CI_REGISTRY_USER }}
          password: ${{ secrets.CI_REGISTRY_TOKEN }}

      - name: Pack gate (npm pack --dry-run assertions)
        run: docker run --rm "${REGISTRY}/${REGISTRY_ORG}/${CLI_CI_IMAGE_REPOSITORY}:${{ needs.image.outputs.image_tag }}" /tests/test.sh tests/pack.test.ts

  # LF-only house rule gate.
  line-endings:
    name: cli / line-endings
    needs: [image, expand]
    runs-on: self-hosted
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4

      - name: Login to registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ secrets.CI_REGISTRY_USER }}
          password: ${{ secrets.CI_REGISTRY_TOKEN }}

      - name: Line endings gate
        run: docker run --rm "${REGISTRY}/${REGISTRY_ORG}/${CLI_CI_IMAGE_REPOSITORY}:${{ needs.image.outputs.image_tag }}" /tests/test.sh tests/line-endings.test.ts

  # tsc --noEmit for the CLI.
  typecheck:
    name: cli / typecheck
    needs: [image, expand]
    runs-on: self-hosted
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4

      - name: Login to registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ secrets.CI_REGISTRY_USER }}
          password: ${{ secrets.CI_REGISTRY_TOKEN }}

      - name: Typecheck
        run: docker run --rm "${REGISTRY}/${REGISTRY_ORG}/${CLI_CI_IMAGE_REPOSITORY}:${{ needs.image.outputs.image_tag }}" /tests/typecheck.sh

  # Summary gate: the full vitest suite (including tests/ci-matrix.test.ts).
  # Runs in parallel with the per-group gates; it does not depend on the
  # smoke matrix, which can legitimately spawn zero jobs.
  test:
    name: cli / test
    needs: [image, expand]
    runs-on: self-hosted
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4

      - name: Login to registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ secrets.CI_REGISTRY_USER }}
          password: ${{ secrets.CI_REGISTRY_TOKEN }}

      - name: Full vitest suite
        run: docker run --rm "${REGISTRY}/${REGISTRY_ORG}/${CLI_CI_IMAGE_REPOSITORY}:${{ needs.image.outputs.image_tag }}" /tests/test.sh

  # Live tests against the dev backend, one matrix job per subcommand. Each
  # job runs its steps sequentially inside a fresh container; persistent
  # agent names (hypercli-ci-<group>-<sub>) keep runs idempotent.
  live:
    name: cli / live / ${{ matrix.sub }}
    needs: [image, expand]
    runs-on: self-hosted
    timeout-minutes: 15
    env:
      HYPER_API_KEY: ${{ secrets.HYPERCLI_DEV_API_KEY }}
      # --dev only retargets the agents API; jobs/flow/files/voice read
      # apiUrl from HYPER_API_BASE and would silently hit prod without it.
      HYPER_API_BASE: https://api.dev.hypercli.com
    strategy:
      fail-fast: false
      matrix:
        include:
          - { group: me, sub: me }
          - { group: configure, sub: configure }
          - { group: skills, sub: list }
          - { group: skills, sub: ls }
          - { group: skills, sub: export }
          - { group: skills, sub: install }
          - { group: agents, sub: ls }
          - { group: agents, sub: list }
          - { group: agents, sub: status }
          - { group: agents, sub: wait }
          - { group: agents, sub: logs }
          - { group: agents, sub: exec }
          - { group: agents, sub: cp }
          - { group: agents, sub: token }
          - { group: agents, sub: models }
          - { group: agents, sub: create }
          - { group: agents, sub: activate }
          - { group: agents, sub: routines }
          # The only job that mutates for real: one serialized lifecycle,
          # persistent name hypercli-ci-lifecycle, cleans up after itself.
          - { group: agents, sub: lifecycle }
          - { group: jobs, sub: gpus }
          - { group: jobs, sub: list }
          - { group: jobs, sub: ls }
          - { group: jobs, sub: get }
          - { group: jobs, sub: logs }
          - { group: jobs, sub: create }
          - { group: jobs, sub: cancel }
          - { group: jobs, sub: extend }
          - { group: jobs, sub: exec }
          - { group: flow, sub: list }
          - { group: flow, sub: status }
          - { group: flow, sub: wait }
          - { group: flow, sub: create }
          - { group: flow, sub: cancel }
          - { group: files, sub: upload }
          - { group: files, sub: get }
          - { group: files, sub: delete }
          - { group: voice, sub: tts }
    steps:
      - uses: actions/checkout@v4

      - name: Login to registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ secrets.CI_REGISTRY_USER }}
          password: ${{ secrets.CI_REGISTRY_TOKEN }}

      - name: Live ${{ matrix.group }} ${{ matrix.sub }}
        run: |
          set -euo pipefail
          docker run --rm \
            -e HYPER_API_KEY \
            "${REGISTRY}/${REGISTRY_ORG}/${CLI_CI_IMAGE_REPOSITORY}:${{ needs.image.outputs.image_tag }}" \
            /tests/live.sh "${{ matrix.group }}" "${{ matrix.sub }}"
