name: SDK Integration Tests

on:
  workflow_call:
  workflow_dispatch:

env:
  GITHUB_RUN_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}

jobs:
  ts-sdk-integration:
    runs-on: self-hosted
    container:
      image: node:22-bookworm
    timeout-minutes: 20
    defaults:
      run:
        shell: bash
        working-directory: ts-sdk
    env:
      TEST_API_KEY: ${{ secrets.TEST_API_KEY }}
      TEST_API_BASE: https://api.dev.hypercli.com
      TEST_AGENT_API_KEY: ${{ secrets.TEST_AGENT_API_KEY }}
    steps:
      - uses: actions/checkout@v4

      - name: Install TS SDK dependencies
        run: npm ci

      - name: Run TS SDK integration tests
        run: npm run test:integration

      - name: Restore runner workspace ownership
        if: always()
        run: |
          chown -R 1000:1000 /__w/hypercli/hypercli /__w/hypercli

  python-sdk-integration:
    runs-on: self-hosted
    container:
      image: python:3.12-bookworm
    timeout-minutes: 20
    defaults:
      run:
        shell: bash
        working-directory: sdk
    env:
      TEST_API_KEY: ${{ secrets.TEST_API_KEY }}
      TEST_API_BASE: https://api.dev.hypercli.com
      TEST_AGENT_API_KEY: ${{ secrets.TEST_AGENT_API_KEY }}
    steps:
      - uses: actions/checkout@v4

      - name: Install Python SDK
        run: |
          python -m venv .venv
          source .venv/bin/activate
          python -m pip install --upgrade pip
          python -m pip install -e ".[dev]"

      - name: Run Python SDK integration tests
        run: |
          source .venv/bin/activate
          pytest -q tests/integration

      - name: Restore runner workspace ownership
        if: always()
        run: |
          chown -R 1000:1000 /__w/hypercli/hypercli /__w/hypercli

  notify-sdk-integration:
    needs:
      - ts-sdk-integration
      - python-sdk-integration
    if: always()
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4

      - name: Notify SDK integration result
        continue-on-error: true
        env:
          NOTIFY_URL: https://api.hypercli.com/notify
          NOTIFY_API_KEY: ${{ secrets.NOTIFY_API_KEY }}
        run: |
          if [ "${{ needs.ts-sdk-integration.result }}" = "success" ] && [ "${{ needs.python-sdk-integration.result }}" = "success" ]; then
            STATUS="success"
            MESSAGE="SDK integration tests passed"
          else
            STATUS="failure"
            MESSAGE="SDK integration tests failed"
          fi

          python3 .github/scripts/ci_notify.py \
            --category sdk \
            --phase test \
            --status "${STATUS}" \
            --event "${{ github.event_name }}" \
            --message "${MESSAGE}" \
            --workflow "SDK Integration Tests" \
            --channel github \
            --run-url "${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"

      - name: Fail workflow when integration jobs fail
        if: needs.ts-sdk-integration.result != 'success' || needs.python-sdk-integration.result != 'success'
        run: exit 1
